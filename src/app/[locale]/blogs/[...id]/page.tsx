import React from "react";
import Image from "next/image";
import BlogDetails from "../../components/BlogDetails";

const page = () => {
	return (
		<div>
			<BlogDetails />
		</div>
	);
};

export default page;
